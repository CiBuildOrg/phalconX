/* @flow */

import React from 'react'
import PhotoContainer from '@Activity/containers/PhotoContainer'

const PhotoScene = (): React$Element<any> => {
    return (
        <PhotoContainer />
    )
}

export default PhotoScene
