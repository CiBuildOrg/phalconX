/* @flow */

import React from 'react'
import BookContainer from '@Book/containers/BookContainer'

const BookScene = (props) => {
    return (
        <BookContainer />
    )
}

export default BookScene
