/* @flow */

import React from 'react'
import VideoContainer from '@Video/containers/VideoContainer'

const VideoScene = (props) => {
    return (
        <VideoContainer />
    )
}

export default VideoScene
