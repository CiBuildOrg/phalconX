/* @flow */

import React from 'react'
import LoginContainer from '@Account/containers/LoginContainer'

const LoginScene = (): React$Element<any> => {
    return (
        <LoginContainer />
    )
}

export default LoginScene
